import React from 'react';
import { Button, Container } from '@mui/material';

export function HomePage() {


    return (
        <Container maxWidth='xl'>
            <Button variant='contained'>Create new Docker file</Button>
        </Container>
    )
}