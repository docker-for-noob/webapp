import { createTheme, Theme } from '@mui/material';

export const defaultTheme: Theme = createTheme({
  palette: {
    primary: {
      main: '#6563ff',
    },
    secondary: {
      main: '#D8D8FF',
    },
  },
});
